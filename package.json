{
  "name": "@pylogmonmcp/powerpoint-generator",
  "description": "A PowerPoint generator for Model Context Protocol (MCP) using Bun (Note: Unable to read/operate existing presentation)",
  "author": "Pylogmon",
  "license": "MIT",
  "version": "0.0.7",
  "module": "index.ts",
  "type": "module",
  "bin": {
    "mcp-powerpoint-generator": "./dist/index.js"
  },
  "scripts": {
    "dev": "bun run ./src/index.ts",
    "prepublishOnly": "bun run build",
    "build": "bun build ./src/index.ts --external '*' --outdir dist"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/bun": "1.2.8"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.8.0",
    "nanoid": "^5.1.5",
    "pptxgenjs": "^3.12.0",
    "zod": "^3.24.2"
  }
}